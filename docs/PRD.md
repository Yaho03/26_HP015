# PRD — IoT 센서 시계열 분석 및 3D 디지털 트윈 기반 조선소 밀폐공간 모니터링 시스템

| 항목 | 내용 |
|---|---|
| 문서명 | 구현용 상세 PRD.md |
| 제품명 | 조선소 밀폐공간 실시간 안전 모니터링 시스템 |
| 버전 | v1.0 |
| 상태 | MVP 구현 기준 |
| 최종 수정일 | 2026-07-05 |
| MVP 개발 기간 | 2026.07 ~ 2026.10 |
| 대상 사용자 | 조선소 안전 관리자, 밀폐공간 현장 작업자, 작업 감독자 |
| 문서 목적 | 개발자가 센서 수집·경보·위치 추적·시각화 흐름을 구현할 수 있도록 요구사항을 정의 |

---

## 0. 문서 사용 규칙

### 0.1 요구사항 강도

| 표현 | 의미 |
|---|---|
| MUST | MVP 구현에 필수. 미구현 시 MVP 성공 기준을 충족하지 못한다. |
| SHOULD | MVP에 포함하는 것을 원칙으로 하되, 일정상 조정 가능하다. |
| MAY | 선택 구현 또는 향후 확장이다. |
| OUT OF SCOPE | MVP에서 제외한다. |

### 0.2 문서 간 우선순위

1. 본 `PRD.md`
2. 하드웨어 회로 구상 초안
3. 수행계획서·개요서
4. 기타 회의록

### 0.3 면책 범위

본 시스템은 **작업자 안전을 보조하는 모니터링·경보 도구**이다. 최종 대피 판단 및 작업 중지 결정은 현장 관리자에게 있으며, 본 시스템의 경보가 공식 안전 기준을 대체하지 않는다.

---

## 1. 제품 정의

### 1.1 제품 개요

조선소 밀폐공간(탱크 내부, 이중저 구획 등) 작업 시 발생할 수 있는 유해가스 누출·산소 결핍·화재·낙상 위험을 실시간으로 감지하고 관리자 및 작업자에게 즉각 경보를 제공하는 IoT 기반 안전 모니터링 시스템이다.

센서 노드가 수집한 시계열 데이터는 MQTT를 통해 서버로 전송되고, 관리자는 웹 대시보드에서 실시간 환경 현황 및 3D 디지털 트윈 시각화를 통해 밀폐공간 내부 상황을 확인한다.

### 1.2 MVP 목표

MVP는 다음 흐름을 한 번에 시연할 수 있어야 한다.

```text
센서 노드 전원 ON
→ 가스·온습도 데이터 실시간 수집 및 MQTT 전송
→ 서버 수신·저장 및 임계값 비교
→ 위험 등급 판정 및 경보 발령
→ 웨어러블 진동 알림 전달
→ 대시보드 실시간 시각화 및 3D 디지털 트윈 표시
→ 시계열 데이터 조회 및 이벤트 로그 확인
```

### 1.3 제품 성공 정의

| 성공 항목 | 정의 |
|---|---|
| 수집 가능성 | 센서 노드 5종(CO2·CO·H2S·VOC·온습도)이 동시 수집되어 대시보드에 표시된다. |
| 경보 가능성 | 임계값 초과 시 3초 이내에 경보가 발령되고 웨어러블 진동이 작동한다. |
| 위치 추적 가능성 | 웨어러블 노드의 2D 위치가 대시보드에 실시간으로 표시된다. |
| 시각화 가능성 | 3D 디지털 트윈 모형 위에 센서 위치와 위험도가 색상으로 표시된다. |
| 이력 조회 가능성 | 특정 시간 범위의 시계열 데이터를 차트로 조회할 수 있다. |

### 1.4 MVP 범위

**포함:**
- 메인 가스 센서 노드 1개 (CO2·CO·H2S·VOC·온습도)
- 서브 센서 노드 3개 (CO2·온습도)
- 웨어러블 노드 1개 (UWB 위치 추적·낙상 감지·진동 알림)
- 고정 열화상 카메라 1개
- MQTT 기반 실시간 데이터 수집
- 임계값 기반 3단계 위험도 경보
- 웹 대시보드 (실시간 수치·차트·경보 로그)
- 3D 디지털 트윈 시각화 (위치·위험도 표시)
- 시계열 데이터 저장 및 조회

**제외 (OUT OF SCOPE):**
- 산소(O2) 센서 연동 (시뮬레이션 안전 이슈, CO2 역산으로 대체)
- AIS·외부 기상 API 연동
- 다중 현장 동시 모니터링
- 사용자 권한 관리 고도화
- 모바일 앱
- 공식 산업안전보건 보고서 생성
- 자동 대피 유도 시스템 제어

---

## 2. 사용자 정의

| 사용자 유형 | 설명 | 주요 과업 |
|---|---|---|
| **안전 관리자** | 육상 또는 현장 외부에서 대시보드를 모니터링 | 실시간 수치 확인, 경보 수신, 이력 조회 |
| **현장 작업자** | 밀폐공간 내부에서 작업 | 웨어러블 착용, 진동 알림 수신 |
| **작업 감독자** | 현장 외부에서 작업 감독 | 경보 수신, 대피 지시 |

---

## 3. 하드웨어 구성

### 3.1 노드 구성 요약

| 노드 | 수량 | 메인보드 | 측정 항목 | 통신 |
|---|---|---|---|---|
| 메인 가스 센서 노드 | 1 | ESP32 DevKitCV4 | CO2·CO·H2S·VOC/LEL%·온습도·VOC | WiFi → MQTT |
| 서브 센서 노드 | 3 | ESP32 UWB WROOM | CO2·온습도 | WiFi → MQTT + UWB 앵커 |
| 웨어러블 노드 | 1 | ESP32 UWB WROVER | UWB 위치·낙상 감지 | WiFi → MQTT + UWB 태그 |
| 고정 탐지 카메라 | 1 | — | 열화상 (8×8 또는 32×24) | WiFi → MQTT |

### 3.2 센서별 측정 항목

| 센서 | 측정 대상 | 통신 방식 | 전원 |
|---|---|---|---|
| MH-Z19B | CO2 (ppm) | UART | 5V |
| BME680 | 온도(°C)·습도(%)·기압(hPa)·VOC(Ω) | I2C | 3.3V |
| MQ-7 | CO (ppm, 아날로그) | ADS1115 → I2C | 5V |
| MQ-136 | H2S (ppm, 아날로그) | ADS1115 → I2C | 5V |
| MQ-2 | VOC/LEL% (아날로그) | ADS1115 → I2C | 5V |
| MPU-6050 | 6축 가속도·자이로 | I2C | 3.3V |
| DW1000 (내장) | UWB TWR 거리 측정 | SPI (내부) | 3.3V |

### 3.3 UWB 위치 측위 구성

- 앵커(Anchor): 서브 센서 노드 3개 — 고정 위치에 배치
- 태그(Tag): 웨어러블 노드 1개 — 작업자 착용
- 측위 방식: TDoA 또는 TWR 기반 2D 삼변측량
- 측위 정밀도 목표: ±30cm 이내 (실내 기준)

> 앵커 3개로 2D 위치 결정이 가능하다. 3D 측위는 OUT OF SCOPE.

---

## 4. 기능 요구사항

### 기능① 실시간 환경 모니터링

#### 4.1.1 데이터 수집

- MUST: 메인 노드는 CO2·CO·H2S·VOC·온도·습도를 **1초 간격**으로 측정한다.
- MUST: 서브 노드는 CO2·온도·습도를 **5초 간격**으로 측정한다.
- MUST: 모든 측정값은 MQTT로 브로커에 전송한다.
- SHOULD: 측정값에는 노드 ID·타임스탬프(UTC)·배터리 잔량이 포함된다.
- MAY: 전송 실패 시 로컬 버퍼에 최대 100건을 저장하고 재전송한다.

#### 4.1.2 MQTT 토픽 구조

```
sensors/{node_id}/gas       → CO2, CO, H2S, VOC 값
sensors/{node_id}/env       → 온도, 습도, 기압
sensors/{node_id}/status    → 배터리, WiFi RSSI, 업타임
wearable/{node_id}/location → UWB x, y 좌표
wearable/{node_id}/imu      → 가속도, 자이로 (낙상 감지용)
alerts/{node_id}            → 경보 발령 이벤트
```

#### 4.1.3 데이터 저장

- MUST: 서버는 수신 데이터를 시계열 DB에 저장한다.
- MUST: 원시 데이터(1초)와 1분 평균값을 모두 저장한다.
- MUST: 경보 이벤트 로그는 영구 보관한다.
- SHOULD: 원시 데이터는 30일 보관 후 삭제한다.

---

### 기능② 위험도 판정 및 경보

#### 4.2.1 가스 농도 임계값

| 항목 | 정상 | ⚠️ 주의 (Level 1) | 🔶 경고 (Level 2) | 🔴 위험 (Level 3) |
|---|---|---|---|---|
| CO2 | < 1,000 ppm | 1,000–2,000 ppm | 2,000–5,000 ppm | ≥ 5,000 ppm |
| CO | < 25 ppm | 25–50 ppm | 50–200 ppm | ≥ 200 ppm |
| H2S | < 1 ppm | 1–5 ppm | 5–10 ppm | ≥ 10 ppm |
| VOC/LEL% | < 10% | 10–25% | 25–50% | ≥ 50% |
| 온도 | < 35°C | 35–38°C | 38–40°C | ≥ 40°C |

> 임계값은 코드에 하드코딩하지 않는다. DB 또는 설정 파일에서 관리하며 관리자가 수정 가능해야 한다.

#### 4.2.2 낙상 감지 기준

- MUST: MPU-6050 가속도 합성벡터가 **2.5g 이상** 순간 급변 후 **1초 이상 정적 상태** 지속 시 낙상으로 판정한다.
- SHOULD: 낙상 판정 시 즉시 Level 3 경보를 발령한다.
- MAY: 작업자가 오감지를 취소할 수 있는 버튼(하드웨어 또는 앱)을 제공한다.

#### 4.2.3 경보 발령 규칙

- MUST: 임계값 초과 감지 후 **3초 이내** 경보가 발령된다.
- MUST: Level 2 이상 경보 시 웨어러블 진동 모터가 작동한다.
- MUST: Level 3 경보 시 웹 대시보드에 팝업 알림 및 사이렌 알림음이 발생한다.
- SHOULD: 경보 발령 시 노드 위치·측정값·시각이 이벤트 로그에 기록된다.
- SHOULD: 임계값 이하로 복귀 시 경보 해제 이벤트가 기록된다.

#### 4.2.4 경보 진동 패턴

| 위험 등급 | 진동 패턴 |
|---|---|
| Level 1 (주의) | 0.5초 진동 × 2회 |
| Level 2 (경고) | 1초 진동 × 3회, 반복 |
| Level 3 (위험) | 연속 진동, 중단 없음 |

---

### 기능③ 작업자 위치 추적

- MUST: 웨어러블 노드의 2D 위치(x, y)가 대시보드에 실시간으로 표시된다.
- MUST: 위치 업데이트 주기는 **1초 이내**이다.
- SHOULD: 작업자 이동 경로가 일정 시간 동안 궤적으로 표시된다.
- SHOULD: 작업자가 위험 구역(Level 2 이상 경보 발령 노드 반경 2m)에 진입 시 추가 경보를 발령한다.
- OUT OF SCOPE: 3D 위치 추적

---

### 기능④ 웹 대시보드

#### 4.4.1 실시간 모니터링 화면

- MUST: 전체 노드의 현재 측정값이 카드 형태로 표시된다.
- MUST: 위험 등급에 따라 카드 색상이 변경된다 (정상: 초록, 주의: 노랑, 경고: 주황, 위험: 빨강).
- MUST: 경보 발령 중인 노드는 강조 표시된다.
- SHOULD: 각 노드별 최근 1시간 추세 미니 차트가 표시된다.

#### 4.4.2 시계열 차트 화면

- MUST: 노드·항목·시간 범위를 선택하여 시계열 차트를 조회할 수 있다.
- MUST: 차트에 임계값 기준선이 표시된다.
- SHOULD: CSV 내보내기가 가능하다.

#### 4.4.3 이벤트 로그 화면

- MUST: 경보 발령·해제 이력이 시간 역순으로 표시된다.
- MUST: 이벤트별 노드 ID·측정값·등급·발령 시각이 표시된다.
- SHOULD: 날짜·등급·노드 기준 필터가 제공된다.

---

### 기능⑤ 3D 디지털 트윈 시각화

- MUST: 시뮬레이션 모형(밀폐공간 축소 재현)을 기반으로 한 3D 공간 모델이 표시된다.
- MUST: 각 센서 노드 위치가 3D 공간 위에 마커로 표시된다.
- MUST: 마커 색상은 위험도 등급에 따라 변경된다.
- SHOULD: 웨어러블 노드(작업자)의 위치가 3D 공간 위에 실시간으로 이동한다.
- SHOULD: 특정 노드 마커 클릭 시 현재 측정값 팝업이 표시된다.
- MAY: 공간 내 가스 농도를 색상 히트맵으로 표시한다.

---

## 5. 비기능 요구사항

### 5.1 성능

| 항목 | 목표 |
|---|---|
| 센서 → 대시보드 표시까지 지연 | ≤ 3초 |
| 임계값 초과 → 경보 발령까지 | ≤ 3초 |
| 대시보드 페이지 로드 | ≤ 2초 |
| 시계열 차트 조회 (7일 데이터) | ≤ 5초 |

### 5.2 가용성

- SHOULD: WiFi 재연결 실패 시 센서 노드는 로컬 LED로 연결 상태를 표시한다.
- SHOULD: MQTT 브로커 재시작 후 노드가 자동으로 재접속한다.
- MAY: 서버 장애 시 최근 경보 이력이 로컬 스토리지에 보존된다.

### 5.3 보안

- SHOULD: MQTT 브로커는 인증(username/password) 또는 TLS를 적용한다.
- SHOULD: 대시보드 접근은 로그인 인증이 필요하다.
- MAY: 노드별 인증 토큰을 발급한다.

---

## 6. 데이터 흐름

```
[센서 노드]
   MH-Z19B, BME680, MQ-7, MQ-136, MQ-2
        │ UART / I2C / ADS1115
   [ESP32]
        │ WiFi
   [MQTT 브로커]
        │
   [백엔드 서버]
    ├─ 시계열 DB 저장
    ├─ 임계값 비교 → 경보 발령
    └─ WebSocket → [웹 대시보드]
                       ├─ 실시간 카드
                       ├─ 시계열 차트
                       ├─ 이벤트 로그
                       └─ 3D 디지털 트윈

[웨어러블 노드]
   MPU-6050 → 낙상 판정
   DW1000 UWB ↔ 서브 노드 앵커 3개 → 2D 위치 계산
        │ WiFi
   [MQTT 브로커] → [백엔드] → [대시보드 위치 표시]
        │
   [MQTT 경보 수신] → 진동 모터 작동
```

---

## 7. 마일스톤

| 시점 | 산출물 |
|---|---|
| 2026.07 | 하드웨어 조립 완료, 센서 노드 MQTT 전송 검증 |
| 2026.08 | 백엔드 서버·DB 구축, 대시보드 기본 화면 (기능①·②) |
| 2026.09 | UWB 위치 추적 연동, 3D 디지털 트윈 초안 (기능③·⑤) |
| 2026.10 | 전체 통합 시연 (시뮬레이션 모형 기준), 이벤트 로그·이력 조회 완성 |

---

## 8. 미결 사항 (Open Questions)

| ID | 항목 | 담당 | 상태 |
|---|---|---|---|
| OQ-1 | UWB 모듈 단품 가격 확인 (121,000원이 묶음인지 단품인지) | 하드웨어 담당 | 미해결 |
| OQ-2 | 고정 탐지 카메라 해상도 결정 (MLX90640 32×24 vs AMG8833 8×8) | 팀 논의 | 미해결 |
| OQ-3 | 시뮬레이션 모형 재질·크기 확정 | 팀 논의 | 미해결 |
| OQ-4 | MQTT 브로커 설치 위치 결정 (로컬 PC vs 클라우드) | 개발 담당 | 미해결 |
| OQ-5 | 3D 디지털 트윈 구현 기술 선택 (Three.js / Unity WebGL 등) | 개발 담당 | 미해결 |
| OQ-6 | 시계열 DB 선택 (InfluxDB / TimescaleDB / SQLite) | 개발 담당 | 미해결 |
