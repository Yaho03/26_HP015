[pytest]
testpaths = tests
asyncio_mode = auto
# 테스트 실행 시 app 패키지를 import 가능하게 (backend/ 를 sys.path 에 추가).
pythonpath = .
# 비동기 fixture/function deprecated 경고 억제.
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
