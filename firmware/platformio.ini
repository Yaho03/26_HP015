; PlatformIO project skeleton.
; Build environments (sensor-node / wearable-node) are configured in issue #36.

[platformio]
src_dir = src

[env]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
