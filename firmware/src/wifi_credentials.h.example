#pragma once

// 실제 자격증명은 firmware/src/wifi_credentials.h 로 복사해서 채운다.
// 이 파일(wifi_credentials.h)은 .gitignore에 등록되어 있어 커밋되지 않는다.

#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

#define MQTT_HOST     "192.168.0.100"
#define MQTT_PORT     1883
#define MQTT_USERNAME "hp015"
#define MQTT_PASSWORD "your-mqtt-password"

// 노드마다 고유해야 한다 (sensor-01 ~ sensor-04 등, 04_DATA_CONTRACT.md 3.1).
#define NODE_ID       "sensor-01"
